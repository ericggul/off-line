import styled from "styled-components";
import { FlexCenterStyle, WholeContainer } from "static/styles";

export const StyledBascilica = styled.div`
  ${WholeContainer};
  ${FlexCenterStyle};

  img {
    max-width: 100%;
    max-height: 100%;
  }
`;
