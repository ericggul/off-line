import styled from "styled-components";
import { FlexCenterStyle, WholeContainer } from "static/styles";

export const StyledEpsilon = styled.div`
  ${FlexCenterStyle};
  ${WholeContainer};
  font-size: 1.5rem;
  text-align: center;
  flex-direction: column;

  h1 {
    margin: 0;
  }
`;
