import styled from "styled-components";
import { FlexCenterStyle, WholeContainer } from "static/styles";

export const StyledNoIsNo = styled.div``;

export const NoIsNo = styled.div`
  ${FlexCenterStyle};
  width: 300px;
  height: 100px;
  background: rgba(232, 79, 71, 0.6);
  font-weight: extra-bold;
  color: white;
`;
