---
to: src/containers/<%= h.changeCase.pascalCase(name) %>/styles.js
---
import styled from 'styled-components';
import {FlexCenterStyle, WholeContainer} from 'static/styles';

export const Styled<%= h.changeCase.pascalCase(name) %> = styled.div`

`;
